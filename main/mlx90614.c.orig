#include "mlx90614.h"
#include "esp_system.h"
#include "esp_random.h"

esp_err_t mlx90614_init(void) {
    // Stub: Initialize I2C here
    return ESP_OK;
}

esp_err_t mlx90614_read_temp(float *temp) {
    // Stub: Replace with actual I2C temperature read
    *temp = 25.0 + (esp_random() % 1000) / 100.0;  // Simulated value
    return ESP_OK;
}
